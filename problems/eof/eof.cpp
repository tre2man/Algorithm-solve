#include<stdio.h>

int main()
{
	char ch;
	int a,w;
	char array[2];

	
	scanf("%d",&w);
	getchar();
	for(a=0;a<w;a++);
	{
		ch=getchar();
		array[a];
	}
	
	for(a=0;a<w;a++)
	{
		printf("%c",array[a]);
	}
	
	return 0;
}
